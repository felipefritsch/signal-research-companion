# Companion project guidance

This is a private, separate teaching project. Start with START_HERE.md and docs/LEARNING_RECORD.md. Keep the copied original guide unchanged; add explanations and cross-references in docs instead. Do not change the sibling quant-research-agent study or synced project sources.

Default to guided co-building around one concept, with a walkthrough-only option when Felipe is tired. Full later lessons are expanded as reached. Checkpoints are optional. Never infer learner mastery from generated material or executed tests.

Default to offline fixtures. A live provider experiment needs explicit task/model/provider choices and an approved spending budget; the current budget is unset. Never print or save API keys. No trading or deployment is in scope.

Keep scripted policies, synthetic counterexamples, public evidence and live measurements distinct. The visible transfer fixtures are consumed diagnostic examples. Current source downloads do not certify historical versions. The teaching harness is not an untrusted-code sandbox.

Run relevant unit tests and notebooks after implementation changes. Preserve old run directories; regenerate reports through scripts/render_report.py. Record failed or superseded experiments. Keep notebook outputs saved and source/version evidence inspectable.
